name: CI

on:
    push:
    pull_request:

env:
    APP_ENV: production

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v7.0.0

            - name: install uv
              uses: astral-sh/setup-uv@v8.2.0
              with:
                  enable-cache: true
                  cache-dependency-glob: 'uv.lock'

            - name: install dependencies
              run: uv sync --all-extras --dev

            - name: run ruff lint
              run: uv run ruff check .

            - name: run ruff format check
              run: uv run ruff format --check .
    test:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                python-version: ['3.12', '3.13', '3.14']
        steps:
            - uses: actions/checkout@v7.0.0

            - name: setup runner environment
              uses: ./.github/actions/setup-python-env
              with:
                  python-version: ${{ matrix.python-version }}

            - name: run test with coverage
              run: uv run pytest --html=report.html --cov=src --cov-report=term-missing

            - name: upload test report
              uses: actions/upload-artifact@v7.0.1
              if: always()
              with:
                  name: test-report-${{ matrix.python-version }}-${{ github.sha }}
                  path: report.html
    build:
        runs-on: ubuntu-latest
        needs: [lint, test]
        permissions:
            contents: read
            packages: write

        steps:
            - uses: actions/checkout@7.0.0

            - name: log into GHCR
              uses: docker/login-action@v4.2.0
              with:
                  registry: ghcr.io
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and push image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  push: true
                  tags: ghcr.io/${{ github.repository }}:${{ github.sha }}
        # deploy-staging:
        # runs-on: ubuntu-latest
        # needs: test # only runs if test job passes
        # environment: staging # pulls secrets/vars from staging environment

        # steps:
        #     - uses: actions/checkout@v7.0.0

        #     - name: Print staging config
        #       run: |
        #           echo "Environment: ${{ vars.APP_ENV }}"
        #           echo "Database URL exists: ${{ secrets.DATABASE_URL != '' }}"
        #           echo "Deployed by: ${{ github.actor }}"
        #           echo "Commit: ${{ github.sha }}"
